// Web app test setup. Loads jest-dom matchers.
// Node 20+ already exposes TextEncoder/TextDecoder/setImmediate globally, so
// no Node-style polyfills are needed here.
import '@testing-library/jest-dom';
